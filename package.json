{
  "name": "automated-line-3d-map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive 3D map of a Total Lab Automation line",
  "scripts": {
    "dev": "vite --host --port 5173",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
